КМ - контекстное меню

X- добавить валидацию при создании.
x- добавить возможность обновления (через тот же диалог, что и создание) по двойному клику на строке + через контекстное меню от строки.
x- добавить команды "Загрузить из файла" и "Сохранить в файл" со стандартным диалогом выбора файла.
- добавить команды "Загрузить из файла (xml)" и "Сохранить в файл (xml)" со стандартным диалогом выбора файла.
- добавить команды "Загрузить из файла (sqllite)" и "Сохранить в файл (sqllite)" со стандартным диалогом выбора файла.
x- команду "вывести список работников привязанных к менеджеру" делаем через контекстное меню (КМ)
X- команду "удалить" делаем через контекстное меню (КМ)
x- команда "вывести список работников привязанных к менеджеру" должна открывать новый диалог со списком работников
x- добавить основное меню (команды загрузки, сохранения, сортировки)
X- использовать для дат тип QDate


std::vector< Emplyees* > loadEmployees()
{
   std::vector< Emplyees* >  employeesVec;


    QSqlQuery query("SELECT country, bla, blabla FROM artist");
    while (query.next())
    {
        Emplyee emp;

        emp.name = query.value(0).toString();
        emp.lastName = query.value(1).toString();
        ....

        employeesVec.push_back( emp );
    }

    return employeesVec;
}